#Function assuming pol1 is furthermost left of line...#However, for this, since only comparison of sign, doesn't really matter...function y = side_triangle(pol1, pol2, p)  #Basically just coding the input as point and line segments in correct order  pos_or_not = area_pol([pol1; pol2; p]);  #and then recoding (1/-1) depending on sign  if pos_or_not > 0    y = 1;  elseif pos_or_not < 0    y = -1;  else    y = 0;  endend